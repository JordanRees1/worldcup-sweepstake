# Dependencies — reinstalled fresh in each Docker stage
node_modules

# Previous web build — rebuilt inside the container
web/dist

# Secrets — NEVER bake these into an image
server/.env
.env
.env.*

# Git history, editor config, dev tooling — not needed at runtime
.git
.gitignore
.claude
.vscode
*.log

# Docs — not needed at runtime
docs
